<% provide(:title, 'Profile') %>
<h1><%= yield(:title) %></h1>
<div class="row profile">
  <div class="col-md-12">
  	<% if @user %>
    	<%= render @user %>
  	<% end %>
  	<h3 class="center">Selling</h3>
	<%= render 'shared/feed' %>
  </div>
</div>