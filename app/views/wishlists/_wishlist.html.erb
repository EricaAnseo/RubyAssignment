<div class="col-sm-12">
	<li>
		<%= wishlist.product.prodname %>
		<%= wishlist.product.price %> 
		<div class="col-sm-6">
			<%= button_to "Remove", wishlist, method: :delete, data: { confirm: "You sure?" }, class: "btn btn-primary product-delete" %>
		</div>
	</li>
</div>